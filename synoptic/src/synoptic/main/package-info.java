package synoptic.main;

/**
 * <p>
 * Provides top-level methods for invoking the Synoptic pipeline. Defines
 * command-line options that the Synoptic binary can parse (
 * <code>SynopticOptions</code>), and contains the <code>SynopticMain</code>
 * class. <code>SynopticMain</code> contains the entry-point to the Synoptic
 * binary as well as top-level library routines for (1) parsing logs, (2) mining
 * invariants, and (3) refining/coarsening the model.
 * </p>
 * <p>
 * Also provides corresponding versions of these functions for Perfume through
 * the classes PerfumeMain and PerfumeOptions.
 * </p>
 */
